

一：数组初始化
	1.只有在定义是初始化，以后就不能用。
	2.不能将一个数组赋值给另一个数组。
	3.计算元素个数：sizeof(数组名)/sizeof(元素类型)
	4.c++11数组初始化：
		使用大括号初始化作为一种通用的初始化方式，且可以用于所有的类型。
			1）初始化数组时可以省略等号（=）
			double earing[4] {};
			2)可不在大括号内包含任何东西，这将把所有元素都设置为0；
			3）列表初始化禁止宽窄转换
